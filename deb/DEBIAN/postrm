#!/bin/bash
rm -rf /etc/45drives/server_info
rmdir /etc/45drives > /dev/null 2>&1 || true
rm -rf /opt/45drives/tools
rmdir /opt/45drives > /dev/null 2>&1 || true
mkdir -p /opt
rm /usr/bin/cephfs-dir-stats
rm /usr/bin/dmap
rm /usr/bin/findosd
rm /usr/bin/lsdev
rm /usr/bin/server_identifier
rm /usr/bin/zcreate
